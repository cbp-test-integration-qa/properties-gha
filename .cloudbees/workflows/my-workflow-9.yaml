apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - name: Say hello
        run: |2-
           with:
                args: sh -c "echo Hello from Alpine"
        uses: docker://alpine:3.19
        shell: /bin/sh
