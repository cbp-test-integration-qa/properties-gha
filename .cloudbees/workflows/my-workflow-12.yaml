apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - name: Action
        uses: E2E_publish_evidence_item
  Uma-first:
    steps:
      - name: Final test container
        run: echo "Final - ever success"
        uses: docker://alpine:3.19
        shell: sh
