apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - uses: docker://alpine:3.19
        run: '"success run"'
  container:
    steps:
      - run: '"Good testing success"'
        uses: docker://alpine:3.19
        shell: sh
      - uses: E2E_publish_evidence_item
