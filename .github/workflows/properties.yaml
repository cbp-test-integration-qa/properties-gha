name: Property Verification Workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  PROPERTIES: >
    {
      "TEST_ENV_VALUE_property": { "type": "string", "value": "test value"},
      "RETRY_COUNT_property": { "type": "number", "value": 5678},
      "FEATURE_FLAG_property": { "type": "boolean", "value": false}
    }

jobs:
  test-properties:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: Initialize properties file
        run: |
          echo "$PROPERTIES" > properties.json

      - name: Update properties values
        run: |
          jq '.TEST_ENV_VALUE_property.value = "updated value" |
              .FEATURE_FLAG_property.value = true |
              .RETRY_COUNT_property.value = 9999' properties.json > temp.json
          mv temp.json properties.json

      - name: Parse properties and export env variables
        run: |
          TEST_ENV_VALUE=$(jq -r '.TEST_ENV_VALUE_property.value' properties.json)
          FEATURE_FLAG=$(jq -r '.FEATURE_FLAG_property.value' properties.json)
          RETRY_COUNT=$(jq -r '.RETRY_COUNT_property.value' properties.json)

          echo "TEST_ENV_VALUE=$TEST_ENV_VALUE" >> $GITHUB_ENV
          echo "FEATURE_FLAG=$FEATURE_FLAG" >> $GITHUB_ENV
          echo "RETRY_COUNT=$RETRY_COUNT" >> $GITHUB_ENV

      - name: Validate properties and generate report
        run: |
          REPORT_FILE=validation_report.txt
          STATUS=0

          echo "Property Validation Report" > $REPORT_FILE
          echo "==========================" >> $REPORT_FILE

          echo "String Value: $TEST_ENV_VALUE" >> $REPORT_FILE
          if [ "$TEST_ENV_VALUE" = "updated value" ]; then
            echo "String Validation: PASS" >> $REPORT_FILE
          else
            echo "String Validation: FAIL" >> $REPORT_FILE
            STATUS=1
          fi

          echo "" >> $REPORT_FILE

          echo "Boolean Value: $FEATURE_FLAG" >> $REPORT_FILE
          if [ "$FEATURE_FLAG" = "true" ] || [ "$FEATURE_FLAG" = "false" ]; then
            echo "Boolean Validation: PASS" >> $REPORT_FILE
          else
            echo "Boolean Validation: FAIL" >> $REPORT_FILE
            STATUS=1
          fi

          echo "" >> $REPORT_FILE

          echo "Number Value: $RETRY_COUNT" >> $REPORT_FILE
          if [[ "$RETRY_COUNT" =~ ^[0-9]+$ ]]; then
            echo "Number Validation: PASS" >> $REPORT_FILE
          else
            echo "Number Validation: FAIL" >> $REPORT_FILE
            STATUS=1
          fi

          echo "==========================" >> $REPORT_FILE

          cat $REPORT_FILE

          exit $STATUS

      - name: Upload validation report (artifact)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: property-validation-report
          path: validation_report.txt

      - name: Prepare report for publishing
        if: always()
        run: |
          mkdir public
          cp validation_report.txt public/index.txt

      - name: Upload Pages artifact
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

     
